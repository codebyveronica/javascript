var multiplicando = 5;
var multiplicador = 0;

 while (multiplicador <= 10) {
    var multiplicação = multiplicando * multiplicador;
    console.log(`${multiplicando} x ${multiplicador} = ${multiplicação}`);
    multiplicador++;
 }
